const community = require('./community');
const user = require('./user');
const discussion = require('./discussion');
const tag = require('./tag');
const communityusers = require('./community-users');

module.exports = {
    community,
    user,
    discussion,
    tag,
    communityusers,
}